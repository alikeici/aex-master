generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  id       Int     @id @default(autoincrement())
  name     String
  email    String
  password String
}
// NOT MIGRATED
model cleanings {
  id Int @id @default(autoincrement())
  type String
  comment String
  timeDate String
  status String

  customer Int
  dustAndGrimeSpecialist Int
}